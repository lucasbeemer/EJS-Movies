<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <meta charset="utf-8">
        <title>EJS-Movies</title>
    </head>
    <body>
        <h1>EJS Movies</h1>
        <a href="/movies/new">Add a movie</a>
        <% for(let i = 0; i < movies.length; i++){ %>
            <div class="">
                <hr>
                <h2><%= movies[i].title %></h2>
                <p><%= movies[i].description %></p>
                <i><%= movies[i].genre %></i><br><br>
                <a href="/upvote/<%= movies[i].id %>">Upvote</a>
                <a href="/downvote/<%= movies[i].id %>">Downvote</a>
                <br>
                <b>Votes: <%= movies[i].votes %></b>
            </div>
        <% } %>
    </body>
</html>